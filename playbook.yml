---
- name: Create handlers
  hosts: all
  become: yes
  tasks:
    - name: modify the content
      copy:
        content: "Baba is Mathsun"
        dest: /tmp/data.txt
      notify: restart_apache

  handlers:
    - name: restart_apache
      service:
        name: httpd
        state: restarted
        enabled: yes
