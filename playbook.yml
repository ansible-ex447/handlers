---
- name: Create handlers
  hosts: all
  become: yes
  tasks:
    - copy:
        content: "hello world"
        dest: /tmp/testfile.txt
      notify: Restart appache
  handlers:
    - name: Restart appache
      service:
        name: httpd
        state: restarted
        enabled: yes 
